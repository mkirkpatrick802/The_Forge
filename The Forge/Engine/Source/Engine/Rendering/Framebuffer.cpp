#include "Framebuffer.h"

Engine::Framebuffer::Framebuffer(const Vector2D size, const bool shouldUseRbo): _FboID(0), _RboID(0), _shouldResize(false)
{
    _size = size;
    _shouldUseRbo = shouldUseRbo;

    _texture
}

Engine::Framebuffer::~Framebuffer()
{
    
}

void Engine::Framebuffer::Init()
{
    
}
